# Kopiera till .env och ändra vid behov.
# docker-compose.yml har samma värden som fallback, så appen startar även utan .env.

DB_ROOT_PASSWORD=rootpassword
DB_NAME=otforum
DB_USER=otforum
DB_PASSWORD=otforum
